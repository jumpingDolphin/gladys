# Gladys — Secrets Template
# Copy to openclaw/.env and fill in values: cp .env.example openclaw/.env
# Set permissions: chmod 600 openclaw/.env
# Never commit openclaw/.env — it's in .gitignore.
#
# --- Other secret files (managed by OpenClaw, not editable here) ---
# Model API keys (Anthropic, Gemini): openclaw/agents/main/agent/auth-profiles.json
#   Managed via: openclaw models auth
# Google OAuth credentials:           openclaw/workspace/google_credentials.json
# Google OAuth token + refresh:       openclaw/workspace/google_token.json
#   Managed via: Google OAuth flow in gmail-assistant skill

# Gateway authentication token (generate with: openssl rand -hex 20)
OPENCLAW_GATEWAY_TOKEN=

# Telegram bot token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# Brave Search API key
BRAVE_SEARCH_API_KEY=

# Notion API key (internal integration)
NOTION_API_KEY=

# Google Maps API key (used by goplaces + local-places skills)
GOOGLE_MAPS_API_KEY=

# OpenAI API key (used by image-gen + whisper skills)
OPENAI_API_KEY=

# Todoist
TODOIST_PERSONAL_TOKEN=
TODOIST_WORKSPACE_TOKEN=

# Backup encryption passphrase (generate with: openssl rand -base64 32)
BACKUP_PASSPHRASE=

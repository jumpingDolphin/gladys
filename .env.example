# Gladys — Environment Variables
# Copy to .env and fill in values: cp .env.example .env
# Never commit .env — it's in .gitignore.

# ── Phase 1: Core ───────────────────────────────────────────────────

# Gateway authentication token (generate with: openssl rand -hex 32)
OPENCLAW_GATEWAY_TOKEN=

# OpenClaw state directory (must match repo location)
OPENCLAW_STATE_DIR=~/gladys/openclaw

# Disable Bonjour/mDNS discovery (single-server setup)
OPENCLAW_DISABLE_BONJOUR=1

# Anthropic API key for Claude
ANTHROPIC_API_KEY=

# Telegram bot token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# ── Phase 2: Voice ──────────────────────────────────────────────────

# OPENAI_API_KEY=

# ── Phase 3: Integrations ───────────────────────────────────────────

# NOTION_API_TOKEN=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
